Scanner sc=new Scanner(System.in);
sout(enter array size);
int n=sc.nextInt();
int[] arr=new int[];
sout(enter array element);
for(int i=0;i<n;i++)
arr[i]=sc.nextInt();
int sum=0;
for(int i=0;i<n;i++)
sum=sum+arr[i];
sout(sum);
