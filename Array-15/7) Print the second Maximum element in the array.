int[] max=Integer.MIN_VALUE;
int[] smax=Integer.SMIN_VALUE;
for(int i=0;i<arr.length;i++)
if(arr[i]>max) max=arr[i];
for(int i=0;i<arr.length;i++)
if(arr[i]>smax && arr[i] !=max)
smax=arr[i];
sout(max)
sout(smax)
